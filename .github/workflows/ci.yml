name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  repro:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reproduce the bug and confirm the fix
        run: ./tests/test-repro.sh
